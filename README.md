# Conditional Seq2Seq VAE

Conditional Sequence to Sequence VAE

## Environment

- `Python >= 3.9`
- `torch >= 1.9.1`
- `torchvision` w/ appropriate version
- `nltk` w/ appropriate version

## Usage

- Use `python3 main.py --help` for more details

